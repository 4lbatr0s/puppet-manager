import { medium, extreme } from "./Configurations.js";
/**
 * A js object to map configurations to values..
 */
const ConfigurationMapper = {
  medium,
  extreme
};

export default ConfigurationMapper;
